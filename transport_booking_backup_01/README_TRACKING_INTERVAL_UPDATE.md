# 💰 อัปเดต: เปลี่ยนหน่วยติดตามจากวินาทีเป็นนาที

## 🎯 วัตถุประสงค์
เปลี่ยนหน่วยการตั้งค่า **tracking_refresh_interval** จาก **วินาที** เป็น **นาที** เพื่อ:
- 💵 **ลดค่าใช้จ่าย Google Maps API** อย่างมาก
- 📊 **ลด API calls** จาก 12 ครั้ง/นาที (ทุก 5 วินาที) เป็น 0.5 ครั้ง/นาที (ทุก 2 นาที)
- 🔋 **ประหยัดแบตเตอรี่** อุปกรณ์มือถือ
- 🌐 **ลด Network Traffic** และ Server Load

---

## 📊 เปรียบเทียบ

| รายการ | ก่อนแก้ (วินาที) | หลังแก้ (นาที) | ผลต่าง |
|--------|------------------|----------------|--------|
| **หน่วย** | วินาที (seconds) | นาที (minutes) | - |
| **ค่า Default** | 30 วินาที | 2 นาที | = 120 วินาที (เพิ่ม 4x) |
| **ช่วง Validation** | 5-300 วินาที | 1-60 นาที | = 60-3,600 วินาที |
| **API Calls/ชั่วโมง** | ~120 ครั้ง | ~30 ครั้ง | **ลด 75%** |
| **ค่าใช้จ่าย/วัน** | สูง | ต่ำ | **ประหยัด 75%** |

---

## 🔧 การเปลี่ยนแปลง

### 1. Model: `models/res_users_settings.py`

#### ก่อนแก้:
```python
tracking_refresh_interval = fields.Integer(
    string='ช่วงเวลาติดตาม (วินาที)',
    default=30,
    required=True,
    help='ความถี่ในการอัพเดทตำแหน่งบนแผนที่ (5-300 วินาที)'
)

@api.constrains('tracking_refresh_interval')
def _check_refresh_interval(self):
    if record.tracking_refresh_interval < 5:
        raise ValidationError('⚠️ ช่วงเวลาติดตามต้องไม่น้อยกว่า 5 วินาที')
    if record.tracking_refresh_interval > 300:
        raise ValidationError('⚠️ ช่วงเวลาติดตามต้องไม่เกิน 300 วินาที')
```

#### หลังแก้:
```python
tracking_refresh_interval = fields.Integer(
    string='ช่วงเวลาติดตาม (นาที)',
    default=2,  # ✅ 2 นาที = 120 วินาที
    required=True,
    help='ความถี่ในการอัพเดทตำแหน่งบนแผนที่ (1-60 นาที) เพื่อประหยัดค่าใช้จ่าย Google Maps API'
)

@api.constrains('tracking_refresh_interval')
def _check_refresh_interval(self):
    if record.tracking_refresh_interval < 1:
        raise ValidationError('⚠️ ช่วงเวลาติดตามต้องไม่น้อยกว่า 1 นาที')
    if record.tracking_refresh_interval > 60:
        raise ValidationError('⚠️ ช่วงเวลาติดตามต้องไม่เกิน 60 นาที')
```

---

### 2. Template: `views/tracking_map_template.xml`

#### ก่อนแก้:
```javascript
// อัพเดททุก 5 วินาที
setInterval(updateLocation, 5000);
```

#### หลังแก้:
```javascript
// อัพเดทตาม tracking_refresh_interval (นาที -> มิลลิวินาที)
const refreshMinutes = 2; // Default: 2 นาที
const refreshInterval = refreshMinutes * 60 * 1000; // แปลงเป็นมิลลิวินาที
setInterval(updateLocation, refreshInterval); // 120,000ms = 2 นาที
```

---

### 3. Template: `views/tracking_map_modern.xml`

#### ก่อนแก้:
```javascript
setInterval(updateLocation, 5000); // Update every 5 seconds
```

#### หลังแก้:
```javascript
// อัพเดทตาม tracking_refresh_interval (นาที -> มิลลิวินาที)
const refreshMinutes = 2; // Default: 2 นาที
const refreshInterval = refreshMinutes * 60 * 1000;
console.log(`⏰ Update interval: ${refreshMinutes} minutes (${refreshInterval}ms)`);
setInterval(updateLocation, refreshInterval);
```

---

### 4. Template: `views/tracking_map_food_delivery.xml`

#### ก่อนแก้:
```javascript
const CONFIG = {
    bookingId: ...
    defaultUpdateInterval: 5000, // Default 5 seconds
    ...
};
```

#### หลังแก้:
```javascript
const CONFIG = {
    bookingId: ...
    defaultUpdateInterval: 120000, // Default 2 นาที = 120,000 มิลลิวินาที
    ...
};
```

---

## 🚀 วิธีติดตั้ง

### 1. รัน Batch File (เป็น Administrator)
```cmd
C:\Program Files\Odoo 18.0.20251009\server\custom-addons\transport_booking\UPDATE_TRACKING_INTERVAL.bat
```

### 2. ตรวจสอบผลลัพธ์
1. เข้าระบบ: **http://localhost:8069**
2. Database: **Tewtongdb**
3. เปิด Menu: **จองคิวรถขนส่ง → การตั้งค่า**
4. ตรวจสอบว่าฟิลด์แสดงเป็น **"นาที"** แล้ว

---

## ⚙️ การใช้งาน

### สำหรับผู้ดูแลระบบ

1. เปิด **การตั้งค่าผู้ใช้** (Transport User Settings)
2. ปรับค่า **"ช่วงเวลาติดตาม"**:
   - **1 นาที** = อัพเดทบ่อยมาก (ใช้สำหรับกรณีฉุกเฉิน)
   - **2 นาที** = แนะนำ (ค่า default)
   - **5 นาที** = ประหยัดมาก (สำหรับเส้นทางไกล)
   - **10-60 นาที** = ติดตามระยะยาว

### การคำนวณค่าใช้จ่าย

**ตัวอย่างการคำนวณ:**
```
รถ 1 คัน ขนส่ง 8 ชั่วโมง/วัน

ก่อนแก้ (ทุก 30 วินาที):
- 120 calls/ชั่วโมง × 8 ชั่วโมง = 960 calls/วัน
- 960 calls × 30 วัน = 28,800 calls/เดือน

หลังแก้ (ทุก 2 นาที):
- 30 calls/ชั่วโมง × 8 ชั่วโมง = 240 calls/วัน
- 240 calls × 30 วัน = 7,200 calls/เดือน

ประหยัด: 21,600 calls/เดือน/รถ = 75% 💰
```

---

## 📋 ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | คำอธิบาย | การเปลี่ยนแปลง |
|------|----------|----------------|
| `models/res_users_settings.py` | โมเดล Settings | เปลี่ยนหน่วย, default, validation |
| `views/tracking_map_template.xml` | Template ปกติ | เปลี่ยน setInterval |
| `views/tracking_map_modern.xml` | Template Modern | เปลี่ยน setInterval + log |
| `views/tracking_map_food_delivery.xml` | Template Food Delivery | เปลี่ยน CONFIG.defaultUpdateInterval |
| `UPDATE_TRACKING_INTERVAL.bat` | Update script | สร้างใหม่ |
| `README_TRACKING_INTERVAL_UPDATE.md` | เอกสาร | ไฟล์นี้ |

---

## ✅ ตรวจสอบการทำงาน

### Test Case 1: ตรวจสอบ UI
1. เปิด **Transport User Settings**
2. ✅ **Expected**: Label แสดง "ช่วงเวลาติดตาม (นาที)"
3. ✅ **Expected**: ค่า default = 2

### Test Case 2: ทดสอบ Validation
1. ลองกรอกค่า 0
2. ✅ **Expected**: Error "ต้องไม่น้อยกว่า 1 นาที"
3. ลองกรอกค่า 61
4. ✅ **Expected**: Error "ต้องไม่เกิน 60 นาที"

### Test Case 3: ทดสอบ Tracking Map
1. เปิดหน้า Tracking Map
2. เปิด Browser Console (F12)
3. ✅ **Expected**: เห็น log `⏰ Update interval: 2 minutes (120000ms)`
4. รอ 2 นาที
5. ✅ **Expected**: ตำแหน่งอัพเดท

---

## 💡 คำแนะนำ

### สำหรับการใช้งานจริง

| ประเภทการขนส่ง | แนะนำ (นาที) | เหตุผล |
|----------------|--------------|--------|
| 🚗 Taxi/Grab | 1-2 นาที | ต้องการความแม่นยำสูง |
| 🚚 ขนส่งสินค้า | 2-5 นาที | สมดุลระหว่างแม่นยำและค่าใช้จ่าย |
| 🚛 ขนส่งระยะไกล | 5-10 นาที | ประหยัดค่าใช้จ่าย |
| 📦 จัดส่งพัสดุ | 3-5 นาที | เหมาะสมกับรูปแบบการจัดส่ง |

### เคล็ดลับ
- 🔋 **ประหยัดแบตเตอรี่**: ใช้ 5-10 นาที สำหรับเส้นทางไกล
- 💰 **ลดค่าใช้จ่าย**: ค่า default 2 นาที เหมาะสมสำหรับส่วนใหญ่
- 📊 **Real-time**: ถ้าต้องการติดตามแบบ Real-time ใช้ 1 นาที

---

## ⚠️ ข้อควรระวัง

1. **ข้อมูลเก่า**: ข้อมูล Settings ที่มีอยู่จะยังเป็นหน่วยวินาที ต้องแปลงเป็นนาที (หาร 60)
2. **Template Cache**: หลัง update อาจต้อง Clear browser cache
3. **API Quota**: ตรวจสอบ Google Maps API quota ว่าเพียงพอ

---

## 🐛 Troubleshooting

### ปัญหา: แผนที่ไม่อัพเดท
**วิธีแก้:**
1. เปิด Browser Console (F12)
2. ตรวจสอบ log `⏰ Update interval`
3. ถ้าไม่เห็น log → Clear cache และ refresh
4. ถ้ายังไม่ได้ → ตรวจสอบ Google Maps API key

### ปัญหา: Validation Error
**วิธีแก้:**
- ตรวจสอบว่าค่าอยู่ในช่วง 1-60 นาที
- ถ้ามีข้อมูลเก่าที่เป็นวินาที ให้แปลงเป็นนาทีก่อน

---

## 📞 ติดต่อ

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- 📧 Email: support@example.com
- 📱 Tel: 02-XXX-XXXX

---

**วันที่อัปเดต:** 2025-11-01  
**Version:** 2.0  
**โดย:** Odoo Development Team
