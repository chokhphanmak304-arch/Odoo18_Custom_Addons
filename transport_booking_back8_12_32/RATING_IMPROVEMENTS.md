# ✅ การปรับแต่งระบบ Rating และการลบ Booking

## 📋 การแก้ไขทั้งหมด

### 1️⃣ ลบได้เฉพาะสถานะ "ร่าง" เท่านั้น

**ไฟล์**: `models/vehicle_booking.py`

**เพิ่ม method unlink**:
```python
def unlink(self):
    """ลบได้เฉพาะสถานะ 'ร่าง' เท่านั้น"""
    for record in self:
        if record.state != 'draft':
            raise ValidationError(
                _('ไม่สามารถลบการจองได้!\n'
                  'สามารถลบได้เฉพาะสถานะ "ร่าง" เท่านั้น\n'
                  'การจองนี้อยู่ในสถานะ: %s') % dict(record._fields['state'].selection).get(record.state)
            )
    return super(VehicleBooking, self).unlink()
```

**ผลลัพธ์**:
- ✅ ลบได้: สถานะ "ร่าง" (draft)
- ❌ ลบไม่ได้: สถานะอื่นๆ (confirmed, in_progress, done, cancelled)
- แสดงข้อความแจ้งเตือนที่ชัดเจน

---

### 2️⃣ ซ่อนเมนู "ประเมินความพึงพอใจ"

**ไฟล์**: `views/delivery_rating_views.xml`

**ก่อนแก้**:
```xml
<menuitem id="menu_delivery_rating"
          name="ประเมินความพึงพอใจ"
          parent="menu_vehicle_booking_root"
          action="action_delivery_rating"
          sequence="30"/>
```

**หลังแก้**:
```xml
<!-- Menu Item - ซ่อนเมนูหลัก เข้าถึงผ่านแท็บในการจองเท่านั้น -->
<!--
<menuitem id="menu_delivery_rating"
          name="ประเมินความพึงพอใจ"
          parent="menu_vehicle_booking_root"
          action="action_delivery_rating"
          sequence="30"/>
-->
```

**ผลลัพธ์**:
- ❌ ไม่แสดงเมนูหลัก "ประเมินความพึงพอใจ" ในแถบด้านบน
- ✅ เข้าถึงได้ผ่านแท็บ "⭐ ประเมินความพึงพอใจ" ในฟอร์มการจองเท่านั้น

---

### 3️⃣ ปุ่ม "📝 สร้าง Link ประเมินใหม่" ต้องบันทึกก่อน

**ไฟล์**: `models/delivery_rating.py`

**ก่อนแก้** (บันทึกทันที):
```python
def action_create_rating_link(self):
    # สร้าง rating record ทันที
    rating = self.env['delivery.rating'].create({
        'booking_id': self.id,
    })
    
    # เปิดฟอร์มที่บันทึกแล้ว
    return {
        'res_model': 'delivery.rating',
        'res_id': rating.id,  # มี ID แล้ว = บันทึกแล้ว
        'view_mode': 'form',
    }
```

**หลังแก้** (ต้องบันทึกเอง):
```python
def action_create_rating_link(self):
    # เปิดฟอร์มใหม่พร้อม default values (ยังไม่บันทึก)
    return {
        'res_model': 'delivery.rating',
        # ไม่มี res_id = ยังไม่บันทึก
        'view_mode': 'form',
        'context': {
            'default_booking_id': self.id,  # ส่ง default value
        },
    }
```

**ผลลัพธ์**:
- เปิดฟอร์มใหม่ (unsaved)
- มีข้อมูล booking_id ตั้งต้นให้อัตโนมัติ
- **ผู้ใช้ต้องกดปุ่ม "บันทึก" เอง**
- หลังบันทึกแล้วถึงจะแสดงในรายการ "ประวัติการประเมินทั้งหมด"

---

## 🎯 User Experience ใหม่

### การลบ Booking:

**เมื่อพยายามลบ**:
1. **สถานะ "ร่าง"** → ✅ ลบได้
2. **สถานะอื่นๆ** → ❌ แสดงข้อความ:
   ```
   ⚠️ ไม่สามารถลบการจองได้!
   สามารถลบได้เฉพาะสถานะ "ร่าง" เท่านั้น
   การจองนี้อยู่ในสถานะ: ยืนยันแล้ว
   ```

### การสร้าง Rating Link:

**Flow เดิม** (บันทึกทันที):
```
คลิกปุ่ม → สร้าง record ทันที → แสดงในรายการ → เปิดฟอร์ม
```

**Flow ใหม่** (ต้องบันทึกเอง):
```
คลิกปุ่ม → เปิดฟอร์มใหม่ → ผู้ใช้ตรวจสอบ → กดบันทึก → แสดงในรายการ
```

**ข้อดี**:
- ✅ ผู้ใช้สามารถตรวจสอบก่อนบันทึก
- ✅ ไม่สร้าง record ที่ไม่จำเป็น
- ✅ ควบคุมได้ว่าเมื่อไหร่จะบันทึก
- ✅ สามารถยกเลิกได้โดยไม่บันทึก (ปิดหน้าต่าง)

---

## 📁 ไฟล์ที่แก้ไข

1. **models/vehicle_booking.py**
   - เพิ่ม method `unlink()` เพื่อจำกัดการลบ

2. **models/delivery_rating.py**
   - แก้ไข `action_create_rating_link()` ไม่ให้บันทึกทันที

3. **views/delivery_rating_views.xml**
   - คอมเมนต์เมนูหลัก "ประเมินความพึงพอใจ"

---

## 🚀 ขั้นตอนการใช้งาน

### 1. Restart Odoo
```bash
cd C:\Program Files\Odoo 18.0.20251009\server\custom-addons\transport_booking
restart_odoo_final_update.bat
```

### 2. Upgrade Module
```
Settings → Apps → ค้นหา "transport_booking" → Upgrade
```

### 3. ทดสอบการลบ
1. ลองลบ Booking สถานะ "ร่าง" → ควรลบได้
2. ลองลบ Booking สถานะอื่นๆ → ควรแสดงข้อความเตือน

### 4. ทดสอบสร้าง Rating Link
1. เปิด Booking ที่ `state = 'done'`
2. ไปแท็บ "⭐ ประเมินความพึงพอใจ"
3. คลิก "📝 สร้าง Link ประเมินใหม่"
4. **ตรวจสอบ**:
   - เปิดฟอร์มใหม่
   - มี booking_id ตั้งต้นให้แล้ว
   - **ยังไม่มีในรายการ** (ก่อนบันทึก)
5. **กดปุ่ม "บันทึก"**
6. **ตรวจสอบ**:
   - ฟอร์มมี Link แสดง
   - **แสดงในรายการแล้ว**

---

## 📊 สรุป

| ฟีเจอร์ | ก่อนแก้ | หลังแก้ |
|---------|---------|---------|
| **ลบ Booking** | ลบได้ทุกสถานะ | ลบได้เฉพาะ "ร่าง" |
| **เมนูหลัก Rating** | แสดงในแถบเมนู | ซ่อน (เข้าผ่านแท็บเท่านั้น) |
| **สร้าง Rating Link** | บันทึกทันที | ต้องกดบันทึกเอง |

---

**เวอร์ชัน**: 18.0.2.2.0  
**วันที่อัปเดต**: 27 ตุลาคม 2568  
**สถานะ**: ✅ พร้อมใช้งาน
