✅ FIX: planned_end_date_t ไม่บันทึกค่า - COMPLETED
═══════════════════════════════════════════════════════════════

📋 ปัญหา:
  • ฟิลด์ `planned_end_date_t` (เวลาส่งจริง) ไม่ถูกบันทึกจากแอป
  • ทำให้ไม่แสดงผลในหน้าประวัติการจัดส่ง

🔍 สาเหตุที่พบ:
  1. ใน vehicle_booking.py:
     - `planned_end_date_t` ถูกตั้ง readonly=True
     - ไม่สามารถแก้ไขจากโค้ดหรือ API ได้

  2. ใน delivery_history.py:
     - `planned_end_date_t` ถูกเซตจาก `booking.actual_delivery_time` (ผิด!)
     - ควรได้จาก `booking.planned_end_date_t` แทน
     - ไม่มี `planned_end_date` ในการบันทึก

🔧 วิธีแก้ที่ทำ:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣  vehicle_booking.py (บรรทัด 156-161)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เปลี่ยนจาก:
    planned_start_date_t = fields.Datetime('วันเวลาออกเดินทางจริง', readonly=True, tracking=True)
    planned_end_date_t = fields.Datetime('วันเวลาที่ส่งจริง', readonly=True, tracking=True)

เป็น:
    planned_start_date_t = fields.Datetime('เวลาออกเดินทางจริง', tracking=True)
    planned_end_date_t = fields.Datetime('เวลาส่งจริง', tracking=True)

✅ ผลลัพธ์:
   - ลบ readonly=True ออก
   - ตอนนี้ tracking_controller.py สามารถอัพเดทได้
   - จะบันทึกค่าอย่างถูกต้องเมื่อ status = 'delivered'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣  delivery_history.py - ฟิลด์นิยาม (บรรทัด 51-57)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เพิ่มฟิลด์ใหม่:
    planned_end_date = fields.Datetime('วันเวลาที่วางแผนส่งถึง', tracking=True)

✅ ผลลัพธ์:
   - เก็บวันที่วางแผนส่งแยกจากวันที่เสร็จสิ้นจริง

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3️⃣  delivery_history.py - ฟังก์ชัน create_from_booking
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เปลี่ยนจาก:
    'planned_start_date_t': booking.actual_pickup_time,
    'planned_end_date_t': booking.actual_delivery_time,

เป็น:
    'planned_start_date_t': booking.planned_start_date_t if booking.planned_start_date_t else booking.actual_pickup_time,
    'planned_end_date': booking.planned_end_date,
    'planned_end_date_t': booking.planned_end_date_t if booking.planned_end_date_t else booking.actual_delivery_time,

✅ ผลลัพธ์:
   - ดึงค่าจริงจาก booking.planned_end_date_t
   - ถ้าไม่มีค่าจึงใช้ actual_delivery_time เป็น fallback
   - บันทึกค่าถูกต้องไป delivery_history

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4️⃣  delivery_history_views.xml (List View)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

เพิ่ม width ให้กับแต่ละคอลัมน์เพื่อให้แสดงผลครบ:
    <field name="name" width="12%"/>
    <field name="completion_date" width="13%"/>
    ...ฯลฯ
    <field name="daily_allowance" width="8%"/>

✅ ผลลัพธ์:
   - ค่าเบี้ยเลี้ยงแสดงผลในหน้า List View
   - ไม่ถูกซ่อนด้านขวาสุด

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 tracking_controller.py (บรรทัด 646-648) - ✅ ถูกต้องแล้ว!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    elif status == 'delivered':
        vals['actual_delivery_time'] = datetime.now()
        vals['planned_end_date_t'] = datetime.now()  ← ✅ บรรทัดนี้ถูกต้อง!

📌 ตรวจสอบแล้ว - โค้ดนี้จะอัพเดทค่าเมื่อ status = 'delivered'
   ตราบเท่าที่เอาออก readonly=True

═══════════════════════════════════════════════════════════════

✨ ขั้นตอนถัดไป:
1. รีสตาร์ท Odoo
2. เข้าระบบและทดสอบการจัดส่ง
3. ตรวจสอบว่า `planned_end_date_t` ได้รับค่าแล้ว
4. ตรวจสอบว่าค่าเบี้ยเลี้ยง (daily_allowance) แสดงในประวัติการจัดส่ง

═══════════════════════════════════════════════════════════════
✅ สถานะ: FIXED & READY
═══════════════════════════════════════════════════════════════
