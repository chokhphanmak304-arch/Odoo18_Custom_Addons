# 🔑 วิธีการตั้งค่า Google Maps API Key

## ⚠️ ปัญหา REQUEST_DENIED

หาก error แสดง `REQUEST_DENIED` แปลว่า API Key ไม่มีสิทธิ์ใช้ Directions API

---

## 🚀 วิธีแก้ไข (ทำตามขั้นตอน)

### **ขั้นตอนที่ 1: เข้า Google Cloud Console**

1. ไปที่ https://console.cloud.google.com/
2. เลือก Project หรือสร้าง Project ใหม่

---

### **ขั้นตอนที่ 2: เปิดใช้งาน APIs**

1. ไปที่ **APIs & Services** → **Library**
2. เปิดใช้งาน APIs ต่อไปนี้ (กด Enable ทีละตัว):

   ✅ **Maps JavaScript API** - สำหรับแสดงแผนที่
   ✅ **Directions API** - สำหรับคำนวณเส้นทาง (สำคัญมาก!)
   ✅ **Places API** - สำหรับค้นหาสถานที่
   ✅ **Geocoding API** - สำหรับแปลงที่อยู่เป็นพิกัด

---

### **ขั้นตอนที่ 3: สร้าง API Key**

1. ไปที่ **APIs & Services** → **Credentials**
2. กด **+ CREATE CREDENTIALS** → เลือก **API Key**
3. คัดลอก API Key ที่ได้

---

### **ขั้นตอนที่ 4: จำกัดการใช้งาน API Key (แนะนำ)**

1. กดที่ API Key ที่สร้างขึ้น
2. ในส่วน **API restrictions**:
   - เลือก **Restrict key**
   - เลือก APIs ที่จะใช้:
     - Maps JavaScript API
     - Directions API
     - Places API
     - Geocoding API
3. กด **Save**

---

### **ขั้นตอนที่ 5: อัพเดท API Key ใน Odoo**

แก้ไขไฟล์: `models/vehicle_booking.py`

```python
# แก้ไขบรรทัดนี้
GOOGLE_API_KEY = "YOUR_NEW_API_KEY_HERE"  # ใส่ API Key ที่สร้างใหม่
```

และแก้ไขไฟล์: `static/src/js/google_maps_widget.js`

```javascript
// แก้ไขบรรทัดนี้
const GOOGLE_API_KEY = "YOUR_NEW_API_KEY_HERE";  // ใส่ API Key เดียวกัน
```

---

## 🔄 Restart Odoo

```bash
# หยุด Odoo
Ctrl + C

# เริ่มใหม่
python odoo-bin -c odoo.conf -u transport_booking
```

---

## ✅ ตรวจสอบว่าใช้งานได้

1. เข้าเมนู **จองคิวรถขนส่ง**
2. กรอก:
   - **สถานที่รับสินค้า**: กรุงเทพมหานคร
   - **ปลายทาง**: จังหวัดชลบุรี
3. เข้าแท็บ **🗺️ แผนที่เส้นทาง**
4. ควรเห็นแผนที่พร้อมเส้นทาง

---

## ❓ หากยังใช้งานไม่ได้

### ตรวจสอบ Console (กด F12)

**Error: REQUEST_DENIED**
→ API Key ไม่ถูกต้อง หรือไม่ได้เปิด Directions API

**Error: OVER_QUERY_LIMIT**
→ เกินจำนวนครั้งฟรี ต้อง Enable Billing

**Error: NOT_FOUND**
→ ชื่อสถานที่ไม่ถูกต้อง

---

## 💰 ค่าใช้จ่าย (Google Maps Platform)

- **$200 Free Credit** ต่อเดือน
- **Directions API**: $5 ต่อ 1,000 requests
- **Maps JavaScript API**: $7 ต่อ 1,000 loads
- จำนวนฟรี: ~40,000 requests/month

---

## 🔒 Security Best Practice

**อย่า commit API Key ลง Git!**

แนะนำใช้ Environment Variables:

```python
import os
GOOGLE_API_KEY = os.environ.get('GOOGLE_MAPS_API_KEY', '')
```

```javascript
const GOOGLE_API_KEY = odoo.GOOGLE_MAPS_API_KEY || "";
```

---

## 📞 ติดต่อสอบถาม

หากมีปัญหา ให้ตรวจสอบ:
1. API Key ใช้งานได้จริงหรือไม่
2. เปิด Directions API แล้วหรือยัง
3. Project มี Billing Account หรือไม่ (สำหรับใช้เกิน free tier)

---

🎉 เรียบร้อย!
