# 📍 การติดตามการขนส่ง - Tracking Implementation

## 🎯 สิ่งที่เพิ่มเข้ามา

เมื่อกดปุ่ม **"เริ่มงานขนส่ง"** ในแอป ระบบจะ:

1. ✅ บันทึกรูปถ่ายสินค้าก่อนขนส่ง
2. ✅ เปลี่ยนสถานะเป็น "กำลังขนส่ง" (in_progress)
3. ✅ **สร้างจุดติดตาม GPS เริ่มต้น** ที่ต้นทาง (pickup location)
4. ✅ บันทึกข้อมูลไปยัง Odoo18 ในแท็บ "📍 ติดตามการขนส่ง"

---

## 📝 ไฟล์ที่แก้ไข

### 1. `vehicle_booking.py` - Model

**เพิ่มฟิลด์:**
```python
tracking_ids = fields.One2many('vehicle.tracking', 'booking_id', string='จุดติดตาม GPS')
```

**อัปเดต method `start_job_with_photo`:**
```python
def start_job_with_photo(self, photo_base64):
    # ... (บันทึกรูปและเริ่มงาน)
    
    # 📍 สร้าง tracking record เริ่มต้น (ณ จุดรับสินค้า)
    if self.pickup_latitude and self.pickup_longitude:
        tracking_vals = {
            'booking_id': self.id,
            'driver_id': self.env.user.id,  # ใช้ current user
            'latitude': self.pickup_latitude,
            'longitude': self.pickup_longitude,
            'timestamp': fields.Datetime.now(),
            'address': self.pickup_location or '',
            'notes': 'เริ่มงาน - รับสินค้าที่ต้นทาง',
            'speed': 0.0,
        }
        tracking = self.env['vehicle.tracking'].create(tracking_vals)
```

### 2. `vehicle_booking_views.xml` - View

**อัปเดตแท็บ "📍 ติดตามการขนส่ง":**
```xml
<separator string="📍 จุดติดตาม GPS ทั้งหมด"/>
<field name="tracking_ids" nolabel="1">
    <list string="จุดติดตาม GPS" default_order="timestamp desc">
        <field name="timestamp"/>
        <field name="latitude"/>
        <field name="longitude"/>
        <field name="speed"/>
        <field name="heading"/>
        <field name="is_moving"/>
        <field name="address"/>
        <field name="notes"/>
    </list>
</field>
```

---

## 🔄 วิธีใช้งาน

### 1. รีสตาร์ท Odoo
```batch
cd "C:\Program Files\Odoo 18.0.20251009\server\custom-addons\transport_booking"
restart_odoo_tracking.bat
```

### 2. ทดสอบในแอป Mobile

1. เปิดแอป Flutter
2. เลือกงานที่ต้องการเริ่ม
3. กดปุ่ม "เริ่มงานขนส่ง"
4. ถ่ายรูปสินค้า
5. กดยืนยัน

### 3. ตรวจสอบใน Odoo18

1. เข้า Odoo18: http://localhost:8078
2. ไปที่เมนู: **จัดการการขนส่ง > การจองคิวรถขนส่ง**
3. เปิด booking ที่เริ่มงานแล้ว
4. คลิกแท็บ **"📍 ติดตามการขนส่ง"**
5. จะเห็น:
   - 🗺️ แผนที่แสดงตำแหน่ง Real-time
   - 📊 สถานะการขนส่ง
   - 📍 จุดติดตาม GPS เริ่มต้น (ณ ต้นทาง)

---

## 📊 ข้อมูลที่บันทึก

เมื่อเริ่มงาน ระบบจะบันทึก:

| ฟิลด์ | ค่า | คำอธิบาย |
|-------|-----|----------|
| `booking_id` | ID ของ booking | อ้างอิงถึงงานขนส่ง |
| `driver_id` | User ที่เริ่มงาน | จาก `self.env.user` |
| `latitude` | พิกัด Latitude | จาก `pickup_latitude` |
| `longitude` | พิกัด Longitude | จาก `pickup_longitude` |
| `timestamp` | เวลาปัจจุบัน | เวลาที่เริ่มงาน |
| `address` | ที่อยู่ต้นทาง | จาก `pickup_location` |
| `notes` | "เริ่มงาน - รับสินค้าที่ต้นทาง" | คำอธิบาย |
| `speed` | 0.0 | ความเร็ว (หยุดนิ่ง) |

---

## 🎯 สิ่งที่ควรทำต่อไป

### 1. เพิ่ม GPS Tracking ระหว่างเดินทาง
```dart
// ใน tracking_service.dart
void startTracking(Booking booking) {
  // อัพเดทตำแหน่งทุก 30 วินาที
  _timer = Timer.periodic(Duration(seconds: 30), (timer) {
    _odooService.updateLocation(
      bookingId: booking.id,
      latitude: currentLat,
      longitude: currentLng,
      speed: currentSpeed,
    );
  });
}
```

### 2. แสดงเส้นทางบนแผนที่ Odoo
- อัปเดต `tracking_map_html` ให้แสดงจุดทั้งหมดจาก `tracking_ids`
- เชื่อมจุดเป็นเส้นทาง (polyline)

### 3. แจ้งเตือนเมื่อใกล้ถึงปลายทาง
- คำนวณระยะห่างจากปลายทาง
- แจ้งเตือนเมื่อใกล้ถึง (เช่น < 1 km)

---

## 🐛 การแก้ปัญหา

### ❌ ไม่มีจุด tracking ปรากฏ
**สาเหตุ:** ไม่มี GPS coordinates (pickup_latitude, pickup_longitude)

**วิธีแก้:**
1. ตรวจสอบว่า Geocoding API ทำงานได้
2. ตรวจสอบ Google Maps API key
3. ลอง geocode ด้วยมือ:
```python
cd "C:\Program Files\Odoo 18.0.20251009\server\custom-addons\transport_booking"
python geocode_existing_bookings.py
```

### ❌ Error: driver_id required
**สาเหตุ:** ใช้ `self.driver_id.id` แต่ driver_id เป็น `vehicle.driver` ไม่ใช่ `res.users`

**วิธีแก้:** ใช้ `self.env.user.id` แทน (แก้ไขแล้ว)

---

## ✅ สรุป

เมื่อกดปุ่ม "เริ่มงานขนส่ง" จะมีการ:
1. ✅ บันทึกรูปถ่าย → `pickup_photo`
2. ✅ เปลี่ยนสถานะ → `in_progress`
3. ✅ สร้างจุด tracking → `vehicle.tracking`
4. ✅ แสดงในแท็บ "📍 ติดตามการขนส่ง"

🎉 **พร้อมใช้งานแล้ว!**
