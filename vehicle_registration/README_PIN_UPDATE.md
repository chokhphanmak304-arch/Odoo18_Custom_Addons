# 🔐 อัพเดท: เพิ่มระบบ PIN 6 หลักสำหรับผู้ขับขี่

## 📋 สรุปการเปลี่ยนแปลง (Version 18.0.1.1.0)

### ✨ ฟีเจอร์ใหม่
- เพิ่ม field **PIN 6 หลัก** สำหรับผู้ขับขี่
- PIN ต้อง**ไม่ซ้ำกัน** ในระบบ (Unique Constraint)
- ตรวจสอบว่า PIN เป็น**ตัวเลข 6 หลักเท่านั้น**
- แสดง PIN แบบ **password** (ซ่อนตัวอักษร) เพื่อความปลอดภัย

### 📁 ไฟล์ที่แก้ไข

#### 1. `models/vehicle_models.py`
```python
# เพิ่ม field PIN
pin = fields.Char('PIN 6 หลัก', size=6, required=True, tracking=True, 
                  help='PIN 6 หลักสำหรับการล็อกอิน (ห้ามซ้ำ)')

# เพิ่ม SQL Constraint เพื่อไม่ให้ PIN ซ้ำ
_sql_constraints = [
    ('pin_unique', 'UNIQUE(pin)', '⚠️ PIN นี้ถูกใช้งานแล้ว กรุณาใช้ PIN อื่น'),
]

# เพิ่ม Validation Method
@api.constrains('pin')
def _check_pin(self):
    for record in self:
        if record.pin:
            # ตรวจสอบว่า PIN เป็นตัวเลขเท่านั้น
            if not record.pin.isdigit():
                raise ValidationError('❌ PIN ต้องเป็นตัวเลขเท่านั้น')
            # ตรวจสอบว่า PIN มีความยาว 6 หลัก
            if len(record.pin) != 6:
                raise ValidationError('❌ PIN ต้องเป็นตัวเลข 6 หลักเท่านั้น')
```

#### 2. `views/vehicle_views.xml`
- เพิ่ม field PIN ในฟอร์มผู้ขับขี่ (แสดงแบบ password)
- เพิ่ม field PIN ใน list view (ซ่อนไว้โดยค่าเริ่มต้น)

#### 3. `__manifest__.py`
- อัพเดทเวอร์ชันเป็น `18.0.1.1.0`
- เพิ่มคำอธิบายฟีเจอร์ PIN

---

## 🚀 วิธีการอัพเกรด Module

### ขั้นตอนที่ 1: เปิด Developer Mode
1. ไปที่ **Settings** > **General Settings**
2. เลื่อนลงมาด้านล่างสุด
3. คลิก **Activate the developer mode**

### ขั้นตอนที่ 2: อัพเกรด Module
1. ไปที่ **Apps**
2. ค้นหา `vehicle_registration` หรือ `ลงทะเบียนรถ`
3. คลิกที่ปุ่ม **Upgrade** (หรือ **Update**)
4. รอให้การอัพเกรดเสร็จสมบูรณ์

### ขั้นตอนที่ 3: ตรวจสอบการทำงาน
1. ไปที่ **Fleet** > **ผู้ขับขี่**
2. สร้างหรือแก้ไขข้อมูลผู้ขับขี่
3. จะเห็น field **PIN 6 หลัก** ในฟอร์ม
4. ทดสอบกรอก PIN:
   - ✅ ถูกต้อง: `123456` (ตัวเลข 6 หลัก)
   - ❌ ผิด: `12345` (ไม่ครบ 6 หลัก)
   - ❌ ผิด: `12345a` (มีตัวอักษร)
   - ❌ ผิด: PIN ซ้ำกับผู้ขับขี่คนอื่น

---

## ⚠️ หมายเหตุสำคัญ

### สำหรับข้อมูลผู้ขับขี่เดิม
- ผู้ขับขี่ที่มีอยู่แล้วจะ**ต้องกรอก PIN** เมื่อแก้ไขข้อมูล
- แนะนำให้ Admin กำหนด PIN ให้ผู้ขับขี่ทุกคนก่อน

### ความปลอดภัย
- PIN จะแสดงแบบ **password** (•••••• ) เพื่อป้องกันการมองเห็น
- ในฐานข้อมูลจะเก็บเป็น **plain text** (ถ้าต้องการเข้ารหัส ให้เพิ่มการ hash ใน method `create` และ `write`)

### การใช้งาน PIN
- PIN นี้สามารถนำไปใช้สำหรับ:
  - การล็อกอินในแอพมือถือ
  - การยืนยันตัวตนก่อนรับงาน
  - การเช็คอิน/เช็คเอาท์

---

## 🔄 การ Rollback (ถ้าต้องการยกเลิก)

ถ้าต้องการยกเลิกการเปลี่ยนแปลง:

1. แก้ไขกลับไฟล์ทั้ง 3 ไฟล์
2. ลบ column `pin` จากฐานข้อมูล:
```sql
ALTER TABLE vehicle_driver DROP COLUMN pin;
```
3. Restart Odoo Server
4. Upgrade module อีกครั้ง

---

## 📞 ติดต่อสอบถาม
หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อ:
- Email: support@yourcompany.com
- Line: @yourcompany

---

**สร้างเมื่อ:** 18 ตุลาคม 2025  
**Version:** 18.0.1.1.0  
**โดย:** Claude AI Assistant 🤖
