# 📝 สรุปการเพิ่มฟิลด์ ShipmentInformation - Transport Sync Module

## 📅 วันที่: October 20, 2025
## 📦 Module: transport_sync (Odoo 18)

---

## 🎯 วัตถุประสงค์

เพิ่มฟิลด์ทั้งหมดจาก `shipment_information` ของ API Odoo 14 เข้าสู่ Odoo 18 เพื่อให้สามารถบันทึกข้อมูลต้นทุนการขนส่งแบบครบถ้วน

---

## ✅ ฟิลด์ที่เพิ่มใหม่ทั้งหมด (33 ฟิลด์)

### 🚚 ข้อมูลการขนส่งพื้นฐาน (Basic) - 3 ฟิลด์เพิ่ม
- [NEW] `vehicle_type_id` - Vehicle Type ID จาก Odoo 14
- [NEW] `shipping_cost_m` - ค่าขนส่งพิเศษ
- [EXISTING] pickup_location, destination, vehicle_type_name, distance_km, shipping_cost_raw, shipping_cost

### 👤 ข้อมูลพนักงานและรถ (Vehicle Assignment) - 2 ฟิลด์เพิ่ม
- [NEW] `delivery_employee_id` - Employee ID จาก Odoo 14
- [NEW] `license_plate_id` - License Plate ID จาก Odoo 14
- [EXISTING] delivery_employee_name, license_plate_name

### ⛽ ข้อมูลค่าน้ำมัน (Fuel) - 4 ฟิลด์ใหม่
- `fuel_price_per_liter` - ค่าน้ำมันเชื้อเพลิง (บาท/ลิตร)
- `fuel_consumption_rate` - อัตราสิ้นเปลืองเชื้อเพลิง (กม./ลิตร)
- `fuel_used_per_trip` - จำนวนน้ำมันที่ใช้ต่อเที่ยว (ลิตร)
- `fuel_cost_per_trip` - ค่าน้ำมันต่อเที่ยว (บาท)

### 📉 ข้อมูลค่าเสื่อมราคา (Depreciation) - 5 ฟิลด์ใหม่
- `vehicle` - ต้นทุนค่ารถ (บาท)
- `vehicle_cost` - ต้นทุนค่ารถดอกเบี้ย 12% (บาท)
- `salvage_value` - มูลค่าซาก (บาท)
- `depreciation_period` - ระยะเวลาค่าเสื่อมราคา (ปี)
- `depreciation_per_trip` - ค่าเสื่อมตัวรถต่อรอบ (บาท)

### 💰 ค่าใช้จ่ายประจำปี (Annual Expenses) - 7 ฟิลด์ใหม่
- `annual_vehicle_tax_y` - ค่าภาษีป้ายวงกลม (บาท/ปี)
- `annual_vehicle_tax` - ค่าภาษีป้ายวงกลม (คำนวณ)
- `annual_insurance_class2` - ค่าเบี้ยประกันชั้น 2
- `annual_insurance_class1` - ประกันชั้น 1 (คำนวณ)
- `annual_compulsory_insurance1` - ค่าประกัน พรบ
- `annual_compulsory_insurance` - ค่าประกัน พรบ (คำนวณ)
- `total_depreciation_per_trip` - รวมค่าเสื่อมต่อรอบ (บาท)

### 👷 ข้อมูลค่าแรงงาน (Labor) - 7 ฟิลด์ใหม่
- `labor_costs` - ค่าแรง (บาท)
- `working_days_per_month` - จำนวนวันทำงานต่อเดือน
- `driver_salary` - เงินเดือนพนักงานขับรถ (บาท/เดือน)
- `maintenance_cost` - ค่าซ่อมบำรุง (บาท/เดือน)
- `trips_per_day` - จำนวนรอบที่วิ่ง/วัน
- `labor_cost_per_trip` - ค่าแรงต่อเที่ยว (บาท)
- `total_labor_per_trip` - ค่าแรงต่อรอบไปกลับ (บาท)

### 📋 ค่าใช้จ่ายอื่นๆ (Other Expenses) - 1 ฟิลด์ใหม่
- `other_expenses` - ค่าใช้จ่ายอื่นๆ (บาท/รอบ)

### 📊 สรุปต้นทุนและกำไร (Cost Summary) - 3 ฟิลด์ใหม่
- `total_cost_per_trip` - ราคาต้นทุน (บาท/รอบ)
- `profit_per_trip` - กำไร (บาท/รอบ)
- `profit_per_trip_p` - กำไร% (เปอร์เซ็นต์)

---

## 📂 ไฟล์ที่มีการเปลี่ยนแปลง

### 1. ✏️ models/transport_order.py
**การเปลี่ยนแปลง:**
- เพิ่มฟิลด์ทั้งหมด 33 ฟิลด์ใหม่
- แก้ไข `_process_order()` เพื่อดึงข้อมูลจาก `shipment_information`
- ดึงข้อมูลจาก nested structure ของ API response

**โครงสร้างการดึงข้อมูล:**
```python
shipment_info = order_data.get('shipment_information', {})
basic = shipment_info.get('basic', {})
vehicle_assignment = shipment_info.get('vehicle_assignment', {})
fuel = shipment_info.get('fuel', {})
depreciation = shipment_info.get('depreciation', {})
annual_expenses = shipment_info.get('annual_expenses', {})
labor = shipment_info.get('labor', {})
other_exp = shipment_info.get('other_expenses', {})
cost_summary = shipment_info.get('cost_summary', {})
```

### 2. ✏️ views/transport_order_views.xml
**การเปลี่ยนแปลง:**

#### List View:
- เพิ่มคอลัมน์: `shipping_cost`, `total_cost_per_trip`, `profit_per_trip`, `profit_per_trip_p`
- แสดงข้อมูลต้นทุนและกำไรในตาราง

#### Form View - เพิ่ม Tabs ใหม่:
1. **🚚 ข้อมูลการขนส่ง** (อัพเดท)
   - เพิ่ม vehicle_type_id, delivery_employee_id, license_plate_id
   - เพิ่ม shipping_cost_m

2. **⛽ ข้อมูลน้ำมัน** (ใหม่)
   - ข้อมูลเชื้อเพลิง: fuel_price_per_liter, fuel_consumption_rate
   - การใช้น้ำมัน: fuel_used_per_trip, fuel_cost_per_trip

3. **📉 ค่าเสื่อมราคา** (ใหม่)
   - ต้นทุนรถ: vehicle, vehicle_cost, salvage_value
   - การคำนวณ: depreciation_period, depreciation_per_trip

4. **💰 ค่าใช้จ่ายประจำปี** (ใหม่)
   - ภาษีและประกัน: annual_vehicle_tax_y, annual_vehicle_tax, annual_insurance_class2, annual_insurance_class1
   - ประกันภัยบังคับ: annual_compulsory_insurance1, annual_compulsory_insurance, total_depreciation_per_trip

5. **👷 ค่าแรงงาน** (ใหม่)
   - ข้อมูลพนักงาน: labor_costs, working_days_per_month, driver_salary, maintenance_cost
   - การคำนวณค่าแรง: trips_per_day, labor_cost_per_trip, total_labor_per_trip

6. **📊 สรุปต้นทุนและกำไร** (ใหม่)
   - ค่าใช้จ่ายอื่นๆ: other_expenses
   - สรุปรายได้: total_cost_per_trip, profit_per_trip, profit_per_trip_p
   - แสดงสรุปรายละเอียดเป็นตาราง

---

## 🔄 การ Sync ข้อมูล

### API Response Structure:
```json
{
  "shipment_information": {
    "basic": { ... },
    "vehicle_assignment": { ... },
    "fuel": { ... },
    "depreciation": { ... },
    "annual_expenses": { ... },
    "labor": { ... },
    "other_expenses": { ... },
    "cost_summary": { ... }
  }
}
```

### Mapping ฟิลด์:
ทุกฟิลด์จาก API จะถูก map ตรงกับฟิลด์ใน Odoo 18 โดยตรง

---

## 🚀 วิธีการ Upgrade Module

### 1. Restart Odoo Service
```bash
# Windows
net stop odoo18
net start odoo18

# Linux
sudo systemctl restart odoo18
```

### 2. Upgrade Module
```
Settings → Apps → transport_sync → Upgrade
```

หรือผ่าน Command Line:
```bash
./odoo-bin -u transport_sync -d your_database
```

### 3. ทดสอบการ Sync
1. เปิด Transport Orders
2. คลิกปุ่ม "ซิงค์จาก Odoo 14"
3. ตรวจสอบว่าฟิลด์ใหม่มีข้อมูล

---

## 📊 ตัวอย่างข้อมูลที่แสดง

### หน้า List View:
```
เลขที่ | สาขา | ลูกค้า | ค่าขนส่ง | ต้นทุน | กำไร | กำไร% | ยอดรวม
SO-001 | อยุธยา | ABC | 2,100 | 1,606.61 | 481.98 | 30% | 2.0
```

### หน้า Form View:
แสดงข้อมูลครบถ้วนใน 8 tabs:
1. ข้อมูลการขนส่ง
2. ข้อมูลน้ำมัน
3. ค่าเสื่อมราคา
4. ค่าใช้จ่ายประจำปี
5. ค่าแรงงาน
6. สรุปต้นทุนและกำไร
7. รายการสินค้า
8. ข้อมูลการซิงค์

---

## ⚠️ หมายเหตุสำคัญ

1. **Database Migration**: ต้อง upgrade module เพื่อสร้างคอลัมน์ใหม่ในฐานข้อมูล
2. **ข้อมูลเก่า**: Orders ที่ sync ก่อนหน้านี้จะมีฟิลด์ใหม่เป็น 0 หรือ null
3. **ข้อมูลใหม่**: Orders ที่ sync หลัง upgrade จะมีข้อมูลครบถ้วน
4. **Readonly Fields**: ฟิลด์ ID (vehicle_type_id, delivery_employee_id, license_plate_id) เป็น readonly เพื่อเก็บค่าจาก Odoo 14

---

## 🔍 การตรวจสอบ

### ตรวจสอบว่าฟิลด์ถูกสร้างแล้ว:
```sql
-- PostgreSQL
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'transport_order' 
  AND column_name LIKE '%fuel%'
ORDER BY column_name;
```

### ตรวจสอบว่ามีข้อมูล:
```sql
SELECT 
    name,
    shipping_cost,
    fuel_cost_per_trip,
    total_cost_per_trip,
    profit_per_trip,
    profit_per_trip_p
FROM transport_order
WHERE sync_date >= '2025-10-20'
LIMIT 10;
```

---

## 📈 ประโยชน์ที่ได้รับ

1. ✅ **ข้อมูลครบถ้วน**: มีข้อมูลต้นทุนการขนส่งทุกด้าน
2. ✅ **การวิเคราะห์**: สามารถวิเคราะห์กำไร/ขาดทุนได้
3. ✅ **รายงาน**: สร้างรายงานต้นทุนได้แม่นยำ
4. ✅ **การตัดสินใจ**: มีข้อมูลสนับสนุนการตัดสินใจ
5. ✅ **Integration**: พร้อมสำหรับ integration กับระบบอื่น

---

## 🎯 Next Steps (แนะนำ)

1. **สร้าง Report**: สร้างรายงานวิเคราะห์ต้นทุนและกำไร
2. **Dashboard**: สร้าง dashboard แสดงข้อมูลสรุป
3. **Graph View**: เพิ่ม Graph/Pivot view เพื่อวิเคราะห์ข้อมูล
4. **Automated Sync**: ตั้งเวลา sync อัตโนมัติ
5. **Validation**: เพิ่ม validation rules สำหรับข้อมูลต้นทุน

---

**Version:** 2.0.0  
**Last Updated:** October 20, 2025  
**Status:** ✅ Ready for Testing  
**Module:** transport_sync
