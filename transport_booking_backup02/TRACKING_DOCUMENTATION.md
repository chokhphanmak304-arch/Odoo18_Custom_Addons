# 📍 ระบบติดตามตำแหน่งรถขนส่ง (Vehicle Tracking System)

## ภาพรวม
ระบบติดตามตำแหน่งรถขนส่งแบบ Real-time สำหรับติดตามการเคลื่อนไหวของรถและคนขับในระหว่างการขนส่ง

## คุณสมบัติหลัก

### 1. การตั้งค่าการติดตาม
- **เมนู**: การตั้งค่า → ผู้ใช้และบริษัท → การตั้งค่าการติดตาม
- **ฟีเจอร์**:
  - เปิด/ปิดการติดตามตำแหน่ง
  - ตั้งค่าความถี่การส่งตำแหน่ง (วินาที)
  - เลือกโหมดความแม่นยำ (High/Low)
  - ตั้งค่าการแจ้งเตือน
  - กำหนดประเภทแผนที่
  - ตั้งค่าการเก็บประวัติ

### 2. แผนที่ติดตาม Real-time
- **เมนู**: จองคิวรถขนส่ง → 🗺️ แผนที่ Real-time
- **ฟีเจอร์**:
  - แสดงตำแหน่งรถทั้งหมดที่กำลังเดินทาง
  - อัปเดตอัตโนมัติทุก 30 วินาที
  - แสดงสถิติรถที่เคลื่อนที่/หยุด
  - คลิกที่ marker เพื่อดูรายละเอียด
  - แสดงความเร็วและทิศทาง

### 3. รายการติดตาม
- **เมนู**: จองคิวรถขนส่ง → 📍 ติดตามการขนส่ง
- **ฟีเจอร์**:
  - View แบบ Kanban, List, Form
  - Filter ตามวัน/สัปดาห์
  - Filter รถที่กำลังเคลื่อนที่
  - ดูประวัติการเคลื่อนไหว
  - Export ข้อมูล

## โครงสร้างข้อมูล

### Model: vehicle.tracking
เก็บข้อมูลตำแหน่งของรถ
- `booking_id`: งานที่กำลังทำ
- `driver_id`: คนขับ
- `vehicle_id`: รถ
- `latitude`, `longitude`: พิกัด GPS
- `speed`: ความเร็ว (km/h)
- `heading`: ทิศทาง (degrees)
- `timestamp`: เวลาที่บันทึก
- `is_moving`: สถานะการเคลื่อนที่
- `address`: ที่อยู่

### Model: tracking.settings
การตั้งค่าการติดตามของแต่ละ user
- `user_id`: ผู้ใช้
- `tracking_enabled`: เปิด/ปิดการติดตาม
- `tracking_interval`: ความถี่การส่งข้อมูล
- `high_accuracy`: โหมดความแม่นยำสูง
- `map_type`: ประเภทแผนที่
- `save_history`: บันทึกประวัติ
- `history_retention_days`: เก็บประวัติกี่วัน

## API Endpoints

### 1. อัปเดตตำแหน่ง
```
POST /api/tracking/update_location
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {
    "booking_id": 1,
    "latitude": 13.7563,
    "longitude": 100.5018,
    "accuracy": 10.0,
    "speed": 60.0,
    "heading": 45.0,
    "battery_level": 85.0,
    "address": "ถนนสุขุมวิท กรุงเทพฯ"
  }
}
```

### 2. ดึงตำแหน่งล่าสุด
```
POST /api/tracking/get_latest
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {
    "booking_id": 1
  }
}
```

### 3. ดึงประวัติการเคลื่อนที่
```
POST /api/tracking/get_history
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {
    "booking_id": 1,
    "hours": 24
  }
}
```

### 4. ดึงการตั้งค่า
```
POST /api/tracking/get_settings
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {}
}
```

### 5. อัปเดตการตั้งค่า
```
POST /api/tracking/update_settings
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {
    "tracking_enabled": true,
    "tracking_interval": 30,
    "high_accuracy": true
  }
}
```

### 6. ดึงรายการรถที่เคลื่อนที่
```
POST /api/tracking/get_active_vehicles
Content-Type: application/json

{
  "jsonrpc": "2.0",
  "params": {}
}
```

## การใช้งานใน Flutter App

### 1. ตั้งค่าการติดตาม
```dart
// ในหน้า Settings
final settings = await _odooService.getTrackingSettings();

// อัปเดตการตั้งค่า
await _odooService.updateTrackingSettings({
  'tracking_enabled': true,
  'tracking_interval': 30,
  'high_accuracy': true,
});
```

### 2. ส่งตำแหน่งอัตโนมัติ
```dart
// Background service
Timer.periodic(Duration(seconds: 30), (timer) async {
  if (settings.trackingEnabled) {
    final position = await Geolocator.getCurrentPosition();
    
    await _odooService.updateLocation(
      bookingId: currentBooking.id,
      latitude: position.latitude,
      longitude: position.longitude,
      accuracy: position.accuracy,
      speed: position.speed * 3.6, // m/s to km/h
      heading: position.heading,
    );
  }
});
```

### 3. แสดงแผนที่ในแอป
```dart
// ดึงตำแหน่งล่าสุด
final location = await _odooService.getLatestLocation(bookingId);

// แสดงบน Google Maps
GoogleMap(
  initialCameraPosition: CameraPosition(
    target: LatLng(location.latitude, location.longitude),
    zoom: 15,
  ),
  markers: {
    Marker(
      markerId: MarkerId('vehicle'),
      position: LatLng(location.latitude, location.longitude),
    ),
  },
);
```

## การบำรุงรักษา

### 1. ลบข้อมูลเก่า
ระบบจะลบข้อมูล tracking เก่าอัตโนมัติตามการตั้งค่า `history_retention_days`

ลบด้วยตนเอง:
```python
# ลบข้อมูลเก่ากว่า 30 วัน
self.env['vehicle.tracking'].sudo().cleanup_old_tracking(days=30)
```

### 2. ตรวจสอบประสิทธิภาพ
- ตรวจสอบจำนวนรายการ tracking ต่อวัน
- ตรวจสอบความถี่การอัปเดต
- ตรวจสอบการใช้งาน API

## Security

### Access Rights
- **base.group_user**: อ่าน, เขียน, สร้าง
- **base.group_system**: ทุกสิทธิ์

### API Authentication
- ต้อง authenticate ก่อนเรียก API
- ใช้ session_id สำหรับการยืนยันตัวตน

## Tips & Best Practices

1. **ประหยัดแบตเตอรี่**:
   - ใช้ low accuracy mode เมื่อไม่จำเป็น
   - เพิ่มช่วงเวลา tracking_interval
   - หยุด tracking เมื่องานเสร็จ

2. **เพิ่มประสิทธิภาพ**:
   - ลบข้อมูลเก่าเป็นประจำ
   - ใช้ index ใน database
   - Cache ตำแหน่งล่าสุด

3. **UX ที่ดี**:
   - แสดงสถานะการเชื่อมต่อ
   - แจ้งเตือนเมื่อไม่สามารถส่งข้อมูลได้
   - แสดงเวลาอัปเดตล่าสุด

## Troubleshooting

### ปัญหา: ตำแหน่งไม่อัปเดต
- ตรวจสอบ GPS permission
- ตรวจสอบ internet connection
- ตรวจสอบการตั้งค่า tracking_enabled

### ปัญหา: แผนที่ไม่แสดง
- ตรวจสอบ Google Maps API key
- ตรวจสอบ JavaScript console
- Clear browser cache

### ปัญหา: Battery drain
- ลด tracking_interval
- ปิด high_accuracy mode
- ใช้ geofencing แทน continuous tracking

## ติดต่อสอบถาม
หากมีปัญหาการใช้งาน กรุณาติดต่อทีมพัฒนา
